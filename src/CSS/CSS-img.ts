import {CSSProperties} from "react";

// name : good

const forProfileButton: CSSProperties = {
    width: '6rem',
    height: '4rem',
    backgroundColor: 'inherit',
};

const forParametersArrow: CSSProperties = {
    width: '3rem',
    height: '2rem',
    backgroundColor: 'inherit',
}

const CSSImg = {
    forProfileButton,
    forParametersArrow
};

export default CSSImg;