import {CSSProperties} from "react";

const inputGeneralSettings: CSSProperties = {
    fontFamily: "inherit",
    fontSize: "100%",
    lineHeight: 1.15,
    margin: 0,
    overflow: "visible",
};

const inputList: CSSProperties = {
    display: "none",
};

const CSSInput = {
    inputGeneralSettings,
    inputList,
};

export default CSSInput;